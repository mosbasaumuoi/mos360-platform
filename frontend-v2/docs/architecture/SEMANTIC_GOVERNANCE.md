PURPOSE

Tài liệu này định nghĩa:

semantic governance architecture
runtime integrity principles
lesson evolution governance
mutation safety
compatibility strategy
validation ownership
AI governance rules

để:

MOS360 evolve an toàn
tránh semantic drift
tránh runtime corruption
tránh future CMS chaos
giữ progression-first runtime integrity
1. CORE GOVERNANCE PRINCIPLE

MOS360 là:

SEMANTIC LEARNING RUNTIME PLATFORM

KHÔNG phải:

RAW CONTENT MANAGEMENT SYSTEM

Mọi content cuối cùng PHẢI converge về:

lesson.blocks = []

trước khi render runtime.

2. GOVERNANCE PRIORITY

MOS360 MUST ALWAYS PRIORITIZE:

Runtime Integrity
Semantic Integrity
Progression Continuity
Compatibility Safety
Founder Sustainability

trước:

feature quantity
CMS flexibility
enterprise complexity
authoring freedom vô kiểm soát
3. GOVERNANCE LAYERS
INPUT LAYER

Responsibility: raw human content.

Sources:

Google Sheet
CMS
AI generation
Runtime import
Static content

INPUT LAYER MUST NOT:

render runtime
mutate canonical runtime
bypass validation
NORMALIZATION LAYER

Responsibility:

trim
cast type
inject defaults
canonicalize
hydrate semantic blocks

OUTPUT: canonical lesson object.

NORMALIZATION LAYER MUST NOT:

render UI
fetch UI assets
mutate runtime state
VALIDATION LAYER

Responsibility:

schema validation
semantic integrity checks
progression safety checks
compatibility checks
block validation

VALIDATION LAYER MUST: reject invalid runtime structures.

RUNTIME LAYER

Responsibility:

runtime orchestration
semantic rendering
progression execution
learning flow execution

RUNTIME LAYER MUST NOT:

mutate canonical contracts
rewrite lesson structure
4. SEMANTIC INTEGRITY RULES

MOS360 MUST ALWAYS:

preserve lesson identity
preserve block identity
preserve canonical contracts
preserve deterministic runtime ordering
preserve semantic ownership

MOS360 MUST NEVER:

render raw template fields directly
bypass normalization
bypass validation
mutate runtime structure inside renderer
duplicate ownership layers
5. LESSON EVOLUTION RULES

LESSONS ARE:

EVOLVING RUNTIME ENTITIES

KHÔNG phải:

STATIC HTML CONTENT

Lesson evolution MUST:

preserve compatibility
preserve semantic meaning
preserve runtime continuity
preserve progression structure
6. MUTATION SAFETY RULES

Mọi mutation MUST:

pass normalization
pass validation
preserve compatibility
preserve runtime integrity
preserve progression continuity
ALLOWED MUTATIONS
block addition
block reorder
metadata extension
optional property extension
reinforcement expansion
narrative improvement
RESTRICTED MUTATIONS
lesson identity rewrite
block ownership rewrite
runtime structure bypass
canonical contract bypass
renderer-level mutation
7. COMPATIBILITY STRATEGY

MOS360 MUST SUPPORT:

ATTACHABLE EVOLUTION

NOT:

BIG BANG REWRITE

New runtime capability MUST:

attach safely
preserve old runtime
preserve old lessons
preserve canonical contracts
8. VALIDATION STRATEGY

Validation MUST happen:

BEFORE RUNTIME HYDRATION

Validation MUST check:

required fields
block types
block structure
identity correctness
semantic continuity
progression safety
9. AI GOVERNANCE RULES

AI MUST NOT:

invent contracts
bypass canonical standards
mutate wrong ownership layers
guess runtime architecture
create duplicate runtime systems

AI MUST:

inspect structure
inspect ownership
inspect canonical contracts
inspect runtime flow
modify responsible layer only
10. FUTURE CMS GOVERNANCE

Future CMS MUST:

write directly:

blocks: []

Future CMS MUST NOT:

render raw fields
bypass semantic blocks
bypass normalization
bypass validation
11. MOST IMPORTANT GOVERNANCE RULE

BEFORE BUILDING ANYTHING:

ALWAYS IDENTIFY:

Input Contract
Canonical Contract
Ownership Layer
Runtime Flow
Compatibility Impact
Progression Impact
12. FINAL GOVERNANCE PRINCIPLE

MOS360 SHOULD ALWAYS PROTECT:

simplicity
clarity
runtime integrity
progression continuity
semantic consistency
lightweight evolution

above everything else.