# MOS360
# COMPOSER RUNTIME ARCHITECTURE

STATUS:
🔄 ACTIVE FREEZE

PURPOSE:
Định nghĩa:
- composer architecture
- semantic authoring flow
- block orchestration
- preview ownership
- mutation lifecycle
- runtime-safe authoring

để:
future visual composer không drift khỏi:
semantic runtime direction.

---

# 1. CORE PRINCIPLE

MOS360 Composer là:

```text
LEARNING FLOW ORCHESTRATOR

KHÔNG phải:

PAGE BUILDER

KHÔNG phải:

HTML EDITOR

KHÔNG phải:

NOTION-LIKE CMS
2. COMPOSER RESPONSIBILITY

Composer RESPONSIBLE FOR:

semantic block composition
learning flow orchestration
progression sequencing
narrative pacing
runtime preview
mutation orchestration

Composer MUST NOT:

render raw HTML pages
bypass runtime contracts
bypass validation
bypass semantic governance
3. OFFICIAL AUTHORING FLOW
Author Input
→ Block Composition
→ Semantic Validation
→ Runtime Preview
→ Mutation Validation
→ Save Draft
→ Runtime Persistence
→ Learn Runtime
4. COMPOSER OWNERSHIP
COMPOSER LAYER

RESPONSIBILITY:

authoring UI
block orchestration
sequencing
draft editing

MUST NOT:

mutate runtime directly
bypass validation
bypass lifecycle
VALIDATION LAYER

RESPONSIBILITY:

semantic integrity
compatibility checks
progression safety
runtime-safe mutations
PREVIEW LAYER

RESPONSIBILITY:

simulate learning runtime
preview pacing
preview sequencing
preview continuity

PREVIEW MUST NOT:

mutate persisted runtime
5. BLOCK COMPOSITION PRINCIPLE

MOS360 blocks là:

SEMANTIC LEARNING ENTITIES

KHÔNG phải:

VISUAL DECORATION COMPONENTS
6. AUTHORING PRINCIPLE

Authoring SHOULD FEEL:

lightweight
flow-first
progression-aware
practical
momentum-oriented

Authoring MUST NOT FEEL:

overwhelming
enterprise-heavy
CMS-like
visually noisy
7. PREVIEW PRINCIPLE

Preview MUST simulate:

runtime sequencing
progression flow
continuity rhythm
fatigue density
focus transitions

KHÔNG chỉ:
visual rendering.

8. MUTATION PRINCIPLE

Composer MUST:

preserve lesson identity
preserve compatibility
preserve semantic integrity
preserve progression continuity
9. FUTURE DIRECTION

Future composer SHOULD support:

adaptive sequencing
AI-assisted composition
semantic suggestions
progression optimization
runtime storytelling
cinematic learning pacing
10. FINAL PRINCIPLE

MOS360 Composer tồn tại để:

ORCHESTRATE LEARNING MOMENTUM

KHÔNG phải:

BUILD CONTENT PAGES

---

# KẾT QUẢ ĐẠT ĐƯỢC

MOS360 giờ đã freeze:
# composer philosophy + runtime flow

↓

Đây là thứ cực kỳ quan trọng trước khi:
- build editor UI
- build drag-drop
- build preview engine
- build AI authoring

↓

vì giờ:
composer đã có:
# kiến trúc đúng ngay từ đầu.